{
  "name": "anime-analytics-server-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Vitest unit + route tests for the anime-analytics backend (kept separate so server/ stays untouched)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5",
    "supertest": "^7.0.0",
    "vitest": "^2.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}
